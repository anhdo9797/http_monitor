import 'package:flutter_test/flutter_test.dart';
import 'package:http_monitor/http_monitor.dart';

void main() {
  group('HTTP Monitor', () {
    test('library exports are available', () {
      // This test will be expanded as we implement the library
      expect(true, isTrue);
    });
  });
}
